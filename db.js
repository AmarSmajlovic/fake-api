const users = require('./data/users');
const questions = require('./data/questions');
const answers = require('./data/answers');
const likes = require('./data/likes');
const dislikes = require('./data/dislikes');

module.exports = {
  users,
  questions,
  answers,
  likes,
  dislikes
}